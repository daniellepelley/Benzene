# Benzene 1.0 API-readiness review (2026-07-14)

**Update (later on 2026-07-14): all actionable items in this review are now resolved.**
§0 build blocker — fixed and merged (PR #5). §1a dead code — whole-dead files deleted (PR #4);
the deferred in-file commented blocks (`IBenzeneClientRequest.cs`, both `DictionaryUtils.cs`,
the example `StartUp`s) cleaned in the follow-up hygiene pass. §1b — `BenzeneWorkerStartup2`
renamed to `BenzeneWorkerBuilder`. §2a — maintainer decided **remove**, not ship: the three
`[Obsolete]` members (`UseCorrelationId`, `WithRequestId`, `WithApplication`) are deleted, with
docs/spec/migration-guide updates and CHANGELOG entries. §2b/§2c — maintainer decision: no full
release in the near term (feature development takes priority); staying releasable is the goal,
so no version bump or preview tiering was enacted. The `KafkaClientMiddleware` swallowed-exception
bug was also fixed in the same pass.

Scan of the public surface on `main` (`fa3f86c`) against `work/1.0.0-release-checklist.md` §1.
Method: static analysis only — **no .NET SDK is available in this environment** (egress blocks the
SDK/NuGet), so nothing here was compiled; treat every finding as "verify against a build". This
complements the earlier `work/api-surface-review.md`, focusing on concrete 1.0 red flags that have
appeared since.

## 0. Release blocker (must be green before any 1.0 tag)

- **`main` does not currently build.** `Benzene.Grpc.Test` has 6 compile errors (missing `using`s +
  an ambiguous `HealthCheckResponse`) from the in-flight gRPC enhancement work. Filed as **issue #3**
  with exact per-file fixes. A red default branch blocks the whole release pipeline — fix first.

## 1. Should-fix before 1.0 (concrete, low-risk)

### 1a. Commented-out dead public code in shipped source (~35 lines across ~13 files)

Several shipped files carry commented-out `public class`/`interface`/`static` declarations — dead
code that ships in the source package and reads as unfinished. Removing comment lines is
**zero-compile-risk**. Two categories:

**Whole-file dead code (delete the file, confirm nothing references it):**
- `src/Benzene.AspNet.Core/AspNetRequestMapper.cs`
- `src/Benzene.Aws.Lambda.ApiGateway/Cors/ApiGatewayContextCorsMiddleware.cs`
- `src/Benzene.Aws.Lambda.ApiGateway/Cors/Extensions.cs`
- `src/Benzene.Clients.Aws/Sns/SqsClientRequestMapper.cs` *(also: filename says `Sqs`, the commented type is `SnsClientRequestMapper` — a naming mismatch either way)*
- `src/Benzene.Clients/Common/IClientRequestMapper.cs`
- `src/Benzene.Core.MessageHandlers/Serialization/PayloadSerializer.cs`
- `src/Benzene.Core.Messages/ReflectionMessageSendersFinder.cs`
- `src/Benzene.Extras/Patches/PatchContractResolver.cs`
- `src/Benzene.Extras/Patches/PatchJsonSerializer.cs`

**Commented blocks inside otherwise-live files (delete the dead block only):**
- `src/Benzene.Azure.Function.AspNet/Extensions.cs` (commented `UseHealthCheck`)
- `src/Benzene.Clients/IBenzeneClientRequest.cs` (commented generic client interfaces/impl)
- `src/Benzene.Core.MessageHandlers/Helper/DictionaryUtils.cs` and `src/Benzene.Core/Helper/DictionaryUtils.cs` (commented `Map`/`JsonToDictionary`/`RootWrapper`)

Also the two example `StartUp` files (`examples/Aws/Benzene.Examples.Aws/StartUp*.cs`) are full of
commented-out `.UseX()` lines — not shipped API, but they're the first thing a new user reads, so
worth a tidy pass too.

### 1b. Versioned public type name: `BenzeneWorkerStartup2`

`src/Benzene.SelfHost/BenzeneWorkerBuilder.cs` exposes `public class BenzeneWorkerStartup2`. A `2`
suffix in a public type name is a code smell to lock in at 1.0 — it implies a superseded v1. Rename
to `BenzeneWorkerStartup` (or make it `internal` if it's an implementation detail behind
`IBenzeneWorkerStartup`). Check for a leftover `BenzeneWorkerStartup` v1 to remove.

## 2. Decisions the maintainer needs to make (not mechanical)

### 2a. `[Obsolete]` APIs — ship or remove at 1.0

Three obsolete members, each with a clear "superseded by" message:
- `src/Benzene.Diagnostics/Correlation/Extensions.cs` — `UseCorrelationId` (→ W3C `traceparent`)
- `src/Benzene.Aws.Lambda.Core/LogContextBuilderExtensions.cs` — two methods (→ `UseBenzeneEnrichment`)

**Recommendation:** keep them `[Obsolete]` (not error) for the 1.0 line — the messages are good and
removal is a breaking change you can defer to 2.0. But 1.0 is the last free moment to remove them if
you'd rather not carry them. Pick one and note it in the migration guide.

### 2b. Per-package 1.0 vs `-preview` tiering (checklist §1)

The newest surfaces are materially less battle-tested than `Benzene.Http`/`Benzene.Aws.Lambda.*`:
gRPC (`Benzene.Grpc*`), the streaming fan-in (`Benzene.Core.Middleware/Streaming`, unmerged on PR #2),
schema-compatibility (`Benzene.Schema.OpenApi/Compatibility`, unmerged), and the Azure isolated-worker
/ ServiceBus packages. Consider shipping the mature core at `1.0.0` and keeping the newest at a
`-preview` tag rather than committing to their signatures for a year.

### 2c. Version path

`version.txt` is `0.0.2`; `CHANGELOG.md` has a maintained `[Unreleased]` section (good). The bump to
`1.0.0` is a deliberate release step — out of scope for autonomous change.

## 3. Verified non-issues (so they aren't re-flagged)

- **`NotImplementedException`** appears only in `Benzene.Core.Middleware/NullBenzeneServiceContainer.cs`
  (a Null-Object that intentionally throws for unsupported operations) and
  `Benzene.Grpc.TestHelpers/TestServerCallContext.cs` (a test stub). Both are legitimate patterns, not
  gaps.

## 4. Already in good shape (no action)

- XML doc completion pass is done (`work/xml-documentation-completion-report.md`); most public types
  carry `///` docs, which matters for IntelliSense-driven adoption (checklist §4).
- Test helpers have been extracted out of production packages into dedicated `*.TestHelpers` packages
  (10 of them) — the `test-helpers-refactoring-plan.md` is complete.
- The `ToDelete/` folder is gone (`ToDelete-refactoring-plan.md` complete).
- The startup-unification arc (`AwsLambdaHost<TStartUp>`, `IHostBuilder.UseBenzene<TStartUp>`,
  ASP.NET `WebApplicationBuilder.UseBenzene<TStartUp>`) is on `main`.
- `CHANGELOG.md` follows Keep-a-Changelog; obsoletes carry actionable messages.

## 5. Recommended order of work

1. Fix issue #3 so `main` builds (blocker).
2. Delete the dead commented-out code (§1a) — one low-risk PR, zero compile impact.
3. Rename/relocate `BenzeneWorkerStartup2` (§1b).
4. Decide §2a (obsoletes) and §2b (preview tiering); record in the migration guide.
5. Then proceed through checklist §2–§6 (versioning, coverage gate, docs gate, release).
