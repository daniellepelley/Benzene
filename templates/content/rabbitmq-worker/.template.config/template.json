{
  "$schema": "http://json.schemastore.org/template",
  "author": "Benzene",
  "classifications": ["Benzene", "RabbitMQ", "Worker"],
  "identity": "Benzene.Templates.RabbitMq.Worker",
  "name": "Benzene RabbitMQ Worker",
  "description": "A self-hosted Benzene worker that consumes a RabbitMQ queue (RabbitMQ.Client).",
  "shortName": "benzene.rabbitmq.worker",
  "sourceName": "BenzeneStarter",
  "preferNameDirectory": true,
  "tags": {
    "language": "C#",
    "type": "project"
  },
  "symbols": {
    "IncludeTests": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "true",
      "displayName": "Include unit test project",
      "description": "Include an xUnit test project that routes the demo topic through your handlers, asserting a successful result and a NotFound for an unknown topic."
    }
  },
  "sources": [
    {
      "modifiers": [
        {
          "condition": "(!IncludeTests)",
          "exclude": [ "BenzeneStarter.Tests/**" ]
        }
      ]
    }
  ]
}
