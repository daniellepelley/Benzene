{
  "$schema": "http://json.schemastore.org/template",
  "author": "Benzene",
  "classifications": ["Benzene", "AWS", "Serverless"],
  "identity": "Benzene.Templates.Aws.ApiGateway",
  "name": "Benzene AWS Lambda (API Gateway)",
  "description": "A Benzene service on AWS Lambda, triggered by API Gateway HTTP requests, deployable with AWS SAM.",
  "shortName": "benzene.aws.apigateway",
  "sourceName": "BenzeneStarter",
  "preferNameDirectory": true,
  "tags": {
    "language": "C#",
    "type": "project"
  },
  "symbols": {
    "IncludeTests": {
      "type": "parameter",
      "datatype": "bool",
      "defaultValue": "true",
      "displayName": "Include unit test project",
      "description": "Include an xUnit test project that routes the demo topic through your handlers, asserting a successful result and a NotFound for an unknown topic."
    }
  },
  "sources": [
    {
      "modifiers": [
        {
          "condition": "(!IncludeTests)",
          "exclude": [ "BenzeneStarter.Tests/**" ]
        }
      ]
    }
  ]
}
